; A imagem php:8.3-fpm-alpine vem com upload_max_filesize=2M: logo/capa aceitos até 5MB
; (UpdateTenantRequest) falhavam antes de chegar ao Laravel. Logo + capa juntos cabem em post_max_size.
; O nginx (docker/nginx/default.conf) aceita até 20M.
upload_max_filesize=10M
post_max_size=12M
