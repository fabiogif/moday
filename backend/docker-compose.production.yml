# Produção — Oracle Cloud Compute (ARM64)
# MySQL e Redis em volumes nomeados (dados persistem entre restarts)
# Não exponha MySQL/Redis na internet; apenas Nginx na porta 8000 (proxy do host)

name: moday-backend

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.production
    image: moday-backend-app:latest
    container_name: moday-backend
    restart: unless-stopped
    working_dir: /var/www/html
    environment:
      TZ: America/Sao_Paulo
    env_file:
      - .env
    volumes:
      # Código atualizado via tar no deploy (sem rebuild obrigatório)
      - ./app:/var/www/html/app
      - ./routes:/var/www/html/routes
      - ./config:/var/www/html/config
      - ./database:/var/www/html/database
      - ./resources:/var/www/html/resources
      - ./bootstrap/app.php:/var/www/html/bootstrap/app.php
      - ./bootstrap/providers.php:/var/www/html/bootstrap/providers.php
      - ./storage:/var/www/html/storage
      - ./bootstrap/cache:/var/www/html/bootstrap/cache
      - ./public:/var/www/html/public
    networks:
      - moday-backend
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ['CMD-SHELL', 'php -r "exit(0);"']
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 40s

  nginx:
    image: nginx:1.27-alpine
    container_name: moday-nginx-internal
    restart: unless-stopped
    environment:
      TZ: America/Sao_Paulo
    ports:
      - '8000:80'
    volumes:
      - ./public:/var/www/html/public:ro
      - ./storage/app/public:/var/www/html/storage/app/public:ro
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - moday-backend
    depends_on:
      app:
        condition: service_started

  mysql:
    image: mysql:8.0
    container_name: moday-mysql
    restart: unless-stopped
    # Porta NÃO publicada no host (segurança Oracle Cloud)
    expose:
      - '3306'
    environment:
      TZ: America/Sao_Paulo
      MYSQL_DATABASE: ${DB_DATABASE:-moday_production}
      MYSQL_USER: ${DB_USERNAME:-moday_user}
      MYSQL_PASSWORD: ${DB_PASSWORD:?Defina DB_PASSWORD no .env}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-${DB_PASSWORD}}
    volumes:
      - moday_mysql_data:/var/lib/mysql
      - ./docker/mysql/my.cnf:/etc/mysql/conf.d/moday.cnf:ro
    networks:
      - moday-backend
    command: --default-authentication-plugin=mysql_native_password
    healthcheck:
      test: ['CMD', 'mysqladmin', 'ping', '-h', '127.0.0.1']
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  redis:
    image: redis:7-alpine
    container_name: moday-redis
    restart: unless-stopped
    environment:
      TZ: America/Sao_Paulo
    expose:
      - '6379'
    volumes:
      - moday_redis_data:/data
    networks:
      - moday-backend
    command: redis-server --appendonly yes
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 3s
      retries: 5

  queue-worker:
    image: moday-backend-app:latest
    container_name: moday-queue-worker
    restart: unless-stopped
    working_dir: /var/www/html
    environment:
      TZ: America/Sao_Paulo
    env_file:
      - .env
    volumes:
      - ./app:/var/www/html/app
      - ./routes:/var/www/html/routes
      - ./config:/var/www/html/config
      - ./database:/var/www/html/database
      - ./resources:/var/www/html/resources
      - ./bootstrap/app.php:/var/www/html/bootstrap/app.php
      - ./bootstrap/providers.php:/var/www/html/bootstrap/providers.php
      - ./storage:/var/www/html/storage
      - ./bootstrap/cache:/var/www/html/bootstrap/cache
    networks:
      - moday-backend
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      app:
        condition: service_started
    command: >
      php artisan queue:work redis
      --queue=emails,default
      --sleep=3
      --tries=3
      --max-time=3600
      --timeout=120

networks:
  moday-backend:
    driver: bridge
    name: moday-backend

volumes:
  moday_mysql_data:
    name: moday_mysql_data
  moday_redis_data:
    name: moday_redis_data
